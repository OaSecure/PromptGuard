# Optional local OCR runtime overlay for GPU-capable PaddleOCR smoke tests.
# Install after requirements.txt inside the project venv.
#
# PaddleOCR 3.x needs PaddlePaddle 3.x APIs. PyPI's paddlepaddle-gpu package
# currently stops at 2.6.x, so install from Paddle's CUDA wheel index:
#   python -m pip install -r apps/api/requirements-ocr-gpu.txt
--index-url https://www.paddlepaddle.org.cn/packages/stable/cu118/
--extra-index-url https://pypi.org/simple

paddlepaddle-gpu==3.2.0
numpy==2.3.5
paddleocr==3.7.0
